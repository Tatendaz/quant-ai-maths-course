[project]
name = "quant-ai-maths-course"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "jupyterlab>=4.6.2",
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

"""Week 1 — plot function families.

Goal: three labelled figures — polynomials, exponentials, logarithms — each with a
few family members on shared axes.

    uv run python exercises/week-01/plot_functions.py

Or build the same plots in a notebook: uv run jupyter lab
"""

import numpy as np
import matplotlib.pyplot as plt

# TODO 1: polynomials — plot y = x, x², x³ for x in [-2, 2] on one axes.
#         Where do they all meet? Which grows fastest for |x| > 1?

# TODO 2: exponentials — plot y = 2^x, e^x, 10^x for x in [-2, 3] on one axes.
#         What point do they all share? (Check x = 0.)

# TODO 3: logarithms — plot y = log₂x, ln x, log₁₀x for x in (0, 10].
#         How does each curve relate to its partner in TODO 2? (Mirror across y = x.)

# TODO 4: every axes gets a legend, a title, and axis labels.
#         Finish with plt.show(), or savefig("functions.png") to keep a copy.
